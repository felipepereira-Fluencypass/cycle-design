// AUTO-GENERATED — não edite manualmente
// Fonte: src/icons/_source → npm run build:icons
import { BaseIcon } from '../BaseIcon'
import type { IconProps } from '../types'

/**
 * @example
 * // Decorativo (ícone puramente visual)
 * <MenuIcon size="sm" decorative />
 *
 * // Semântico (requer aria-label)
 * <MenuIcon size="sm" aria-label="descrição do ícone" />
 */
export function MenuIcon(props: IconProps): JSX.Element {
  return (
    <BaseIcon {...props}>
      <path d="M4 6h16M4 12h16M4 18h10"/>
    </BaseIcon>
  )
}
